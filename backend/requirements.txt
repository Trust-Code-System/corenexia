# Corenexia backend — Milestone 1
# Tip: `pip install -U -r requirements.txt`. The Anthropic SDK must be recent enough to support
# claude-opus-4-8 + adaptive thinking + output_config; upgrade if you see parameter errors.

fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.69
google-genai>=1.0  # Gemini provider (optional; only imported when LLM_PROVIDER=gemini)
langgraph>=0.2.40
mcp>=1.2
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9  # OAuth token endpoint (form parsing)
pyjwt>=2.8  # MCP OAuth 2.1 scoped access tokens

# Observability (Initiative B) — OpenTelemetry GenAI tracing + OTLP/HTTP export.
opentelemetry-api>=1.30
opentelemetry-sdk>=1.30
opentelemetry-exporter-otlp-proto-http>=1.30

# dev / test
pytest>=8.0
httpx>=0.27
